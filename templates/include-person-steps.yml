parameters:
    - name : PERSONS 
      type: object
      default: null
steps:
  - task: Bash@3
    displayName: "Test"
    inputs:
      targetType: inline
      script: |
        echo "${{ parameters.PERSONS }}"
     

        
