parameters:
    - name : PERSONS 
      type: object
      default: null
steps:
  - task: 
    displayName: "Test"
    inputs:
      targetType: inline
      script: |
        for person in ${{ parameters.PERSONS }};do
            echo "${{ person.name }} -> ${{ person.age }}"
        done

        
