parameters:
    - name : PERSONS 
      type: object
      default: null
steps:
  - task: Bash@3
    displayName: "Test"
    inputs:
      targetType: inline
      script: |
        IFS="," read -a vms_array <<< ${{ parameters.PERSONS }}
        echo "VMs array is: $vms_array"

        
