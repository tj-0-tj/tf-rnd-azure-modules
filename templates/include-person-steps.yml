parameters:
    - name : PERSONS 
      type: object
      default: null
steps:
  - task: Bash@3
    displayName: "Test"
    inputs:
      targetType: inline
      script: |
        vars=${{ convertToJson(parameters.PERSONS) }}
        echo $vars
        
        
